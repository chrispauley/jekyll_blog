git
# Jekyll Blog
Online code from James Wilkerson's course on Lynda.com

## Requirements
```
ruby -v && gem -v && gcc
```

## Site structure

## Usage
```
jekyll build
- or -
jekyll serve
```

## Publishing
To create an orphan branch so that the source isn't tracked from other branches:
```
git checkout --orphan gh-pages
```

git push origin gh-pages
